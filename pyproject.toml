[project]
name = "cyberslm"
version = "0.3.1"
description = "A private, local-first multimodal cybersecurity assistant for Apple Silicon."
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
  "fastapi>=0.116,<1",
  "httpx>=0.28,<1",
  "pillow>=11,<13",
  "python-dotenv>=1.1,<2",
  "python-multipart>=0.0.20,<1",
  "streamlit>=1.48,<2",
  "uvicorn[standard]>=0.35,<1",
]

[project.scripts]
cyberslm-eval = "cyberslm.evaluation.cli:main"
cyberslm-knowledge = "cyberslm.knowledge.cli:main"

[project.optional-dependencies]
mlx = ["mlx-vlm>=0.3.4,<1"]
dev = [
  "pytest>=8.4,<10",
  "pytest-cov>=6.2,<8",
  "ruff>=0.12,<1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cyberslm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
